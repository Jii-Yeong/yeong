import {
  Meta,
  Controls,
  Primary,
  Story,
  Canvas,
  Source,
} from '@storybook/blocks';
import * as CommonPaginationStories from './CommonPagination.stories';

<Meta
  title="UI/Components/Pagination/CommonPagination"
  of={CommonPaginationStories}
/>

# CommonPagination

일반적으로 사용되는 페이지네이션 컴포넌트 입니다.

## Props

<Canvas of={CommonPaginationStories.Default} />

<Controls of={CommonPaginationStories.Default} />

## Usage

<Source code={`
import { CommonPagination } from '@yeong/ui';

export default function Page() {
  const [pagination, setPagination] = useState(0)

const clickPagination = (value: number) => {
setPagination(pagination)
// 그 외 페이지 클릭 시 발생시킬 이벤트
}

return (
<div>
<CommonPagination totalCount={25} pagination={pagination} onClickPagination={clickPagination} />
</div>
)
}
`} />

## Stories

### Default

<Canvas of={CommonPaginationStories.Default} />
