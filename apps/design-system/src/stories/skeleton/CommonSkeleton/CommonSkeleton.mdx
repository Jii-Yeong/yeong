import { Meta, Controls, Primary, Story, Canvas } from '@storybook/blocks';
import * as CommonSkeletonStories from './CommonSkeleton.stories';

<Meta title="UI/Button/CommonSkeleton" of={CommonSkeletonStories} />

# CommonSkeleton

일반적으로 사용되는 스켈레톤 컴포넌트 입니다.

<Canvas of={CommonSkeletonStories.All} />

## Props

<Canvas of={CommonSkeletonStories.Default} />

<Controls of={CommonSkeletonStories.Default} />

## Stories

### Default

<Canvas of={CommonSkeletonStories.Default} />
