import { Meta, Controls, Primary, Story, Canvas } from '@storybook/blocks';
import * as LoadingSpinnerStories from './LoadingSpinner.stories';

<Meta title="UI/Loading/LoadingSpinner" of={LoadingSpinnerStories} />

# LoadingSpinner

일반적으로 사용되는 로딩 스피너 컴포넌트 입니다.

<Canvas of={LoadingSpinnerStories.All} />

## Props

<Canvas of={LoadingSpinnerStories.Default} />

<Controls of={LoadingSpinnerStories.Default} />

## Stories

### Default

<Canvas of={LoadingSpinnerStories.Default} />

### Custom Color

<Canvas of={LoadingSpinnerStories.CustomColor} />

### Custom Style

<Canvas of={LoadingSpinnerStories.CustomStyle} />
